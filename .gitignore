# Next.js
node_modules/
.next/
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Editor / OS
.vscode/*
!.vscode/settings.json
.vs/
.history/
.idea/
*.log

# OS
Thumbs.db
.DS_Store

# ── 보안: 비밀 키는 절대 커밋하지 않는다 ───────────────────
# OPENAI_API_KEY / ANTHROPIC_API_KEY 등 진짜 시크릿은 .env.local(위 .env*.local 로 제외) 또는
# Vercel 대시보드 Environment Variables 에만 둔다.
# .env.production 은 "공개해도 안전한 값"(Firebase 공개설정·provider·모델)만 담아 커밋된다.
